# Copy to /etc/waterlevels/env on the server.
# chmod 640 /etc/waterlevels/env
# chown root:waterlevels /etc/waterlevels/env
#
# Do NOT quote values — pydantic-settings reads them raw.
WL_DB_PATH=/opt/waterlevels/data/water.db
WL_SYNC_INTERVAL_HOURS=6
WL_ENABLED_COUNTRIES=cy,gr,es,pt
