# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Application data (created at runtime)
data/

# Environment overrides
.env

# Tailwind standalone CLI binary (downloaded, not committed)
tailwindcss

# Tailwind CSS source entry point (generated artifact, not committed)
app/static/css/input.css

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/*.tfvars
terraform/.terraform.lock.hcl
terraform/backend.hcl
terraform/local/.terraform/
terraform/local/terraform.tfstate
terraform/local/terraform.tfstate.backup
terraform/local/.terraform.lock.hcl

# Claude Code
.claude/
.claire/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Node
node_modules/

# Internal docs (moved to private repo)
outreach/
sprints/
research/
